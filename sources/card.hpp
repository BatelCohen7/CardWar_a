#pragma once
#include <iostream>
#include <string.h>

namespace ariel {
    class Card {
        private:
            /* data */
        public:
            Card(/* args */);
            ~Card();
    };
}
